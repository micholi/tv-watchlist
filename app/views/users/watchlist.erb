<div class="main-content">
<h3>My Shows</h3>
</div>

<table>
  <thead>
    <tr>
      <th>Show</th>
      <th>Genre</th>
      <th>Network</th>
    </tr>
  </thead>

  <tbody>
    <% @shows.each do |show| %>
    <tr>
    <td><a href="/shows/<%= show.slug %>"><%= show.name %></td>
    <td><%= show.genre %></td>
    <td><%= show.network.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<div class="links-buttons">
<a class="btn btn-outline-info btn-md" href="/shows/new" role="button">Add New Show</a>
</div>
